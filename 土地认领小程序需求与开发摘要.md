# 土地认领/租赁微信小程序需求与开发摘要

## 项目定位

土地认领/租赁微信小程序，面向公众用户。管理员端单独使用 Web 后台，小程序不做管理员功能。

## 技术与结构

- 技术栈：原生微信小程序
- 项目目录：`miniapp/`
- 结构：`pages/`、`components/`、`api/`、`utils/`、`app.json`
- 已将 `project.config.json` 的 `miniprogramRoot` 指向 `miniapp/`

## 核心功能

- 微信授权登录与手机号绑定
- 首页土地浏览、搜索、筛选
- 土地详情、年租租赁、微信支付
- 我的土地、实时监控、化肥购买、人工预约服务
- 消息通知、个人中心
- 租赁记录、费用账单、消耗品订单、服务预约记录

## 底部 Tab

- 首页
- 我的土地
- 消息
- 我的

## 首页

- 顶部搜索/筛选
- 中间轮播图
- 下方土地卡片列表
- 搜索按土地名称/编号
- 筛选条件：可租状态、价格区间、面积区间、位置区域

## 土地卡片字段

- 土地图片
- 土地名称
- 土地编号
- 面积
- 年租价格
- 可租状态
- 剩余租期

## 土地状态

- 可租
- 已租
- 维护中
- 即将到期
- 已到期

## 土地详情

只展示土地面积、价格、可租状态、土地图片、剩余租期。可租时显示“立即租赁”，已租/维护中按钮置灰。

## 租赁流程

土地详情 → 立即租赁 → 确认订单 → 创建支付 → `wx.requestPayment` → 支付成功页 → 我的土地

## 我的土地详情

入口包括实时监控、购买化肥、预约服务、续租入口、租期信息。不再做远程控制按钮；浇水、施肥都归入人工预约服务。

## 人工预约服务

服务类型：播种、除草、施肥、采摘、翻土、病虫害处理、浇水。

预约字段：地块、服务类型、时间、备注。

服务进度：待受理、已受理、服务中、已完成、已取消。

## 化肥购买

在某个土地下点击购买化肥，先做单商品“化肥”，用户选择数量后支付。单价由管理员 Web 端控制，前端从接口取。

## 消息通知

类型包括租赁到期提醒、支付成功、服务进度、异常告警、系统公告。消息做独立页面，支持列表和详情。

## 个人中心入口

- 我的土地
- 租赁记录
- 费用账单
- 消耗品订单
- 服务预约
- 联系客服
- 设置

客服方式：固定电话、微信二维码。

## 接口约定

- 接口文档来自项目文件夹中的 `默认模块.openapi.json`
- 鉴权：Bearer Token，也就是 JWT
- 登录流程：`wx.login` 获取 `code` → 调后端 `/auth/wx-login` → 后端返回 token 和用户信息 → 未绑定手机号则进入绑定手机号页
- 绑定手机号：使用微信 `open-type="getPhoneNumber"`，将微信返回数据发给后端 `/auth/bind-phone`
- 支付流程：前端创建订单 → 后端返回微信支付参数 → 前端调用 `wx.requestPayment` → 支付后跳转成功页/查询状态

## 已识别核心接口

- `/auth/wx-login`
- `/auth/bind-phone`
- `/user/profile`
- `/lands`
- `/lands/{landId}`
- `/lands/{landId}/claim`
- `/user/lands`
- `/orders`
- `/user/orders`
- `/pay/create`
- `/pay/status/{paymentId}`
- `/notifications`
- `/notifications/{notificationId}/read`
- `/notifications/read-all`

## 当前已开发

已创建 `miniapp/` 前端骨架，包括全局配置、API 请求封装、JWT 请求头、页面路由、公共组件、核心业务页面、本地占位图片、微信支付调用逻辑、登录/绑定手机号逻辑。

## 待后端/联调确认

- `miniapp/api/config.js` 中的 `BASE_URL` 需要替换为真实后端域名
- Apifox 当前 `ServiceType` 似乎只包含 `WATER/FERTILIZE`，但前端已按需求预留更多服务类型，后端需要补枚举或做映射
- 需求中约定监控接口为 `GET /lands/{id}/camera`，但当前 OpenAPI 中主要看到 `/lands/{landId}/monitor`，需后端确认摄像头播放地址接口
- 化肥价格字段需后端明确返回字段，例如 `fertilizePrice` 或 `fertilizerPrice`
- 续租目前只做入口，未接真实支付闭环
