<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wisdom.farm.mapper.AdminUserMapper">
    <resultMap id="AdminUserResultMap" type="com.wisdom.farm.entity.AdminUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByUsername" resultMap="AdminUserResultMap">
        SELECT id, username, password, password_hash, role, status, create_time, update_time
        FROM admin_user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="AdminUserResultMap">
        SELECT id, username, password, password_hash, role, status, create_time, update_time
        FROM admin_user
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="AdminUserResultMap">
        SELECT id, username, password, password_hash, role, status, create_time, update_time
        FROM admin_user
        ORDER BY create_time DESC
    </select>

    <update id="updatePassword">
        UPDATE admin_user
        SET password_hash = #{passwordHash}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE admin_user
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>
