<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wisdom.farm.mapper.LandMapper">
    <resultMap id="LandResultMap" type="com.wisdom.farm.entity.Land">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="location" column="location"/>
        <result property="area" column="area"/>
        <result property="price" column="price"/>
        <result property="description" column="description"/>
        <result property="images" column="images"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectList" resultMap="LandResultMap">
        SELECT id, name, location, area, price, description, images, status, create_time, update_time
        FROM land
        <where>
            <if test="status != null">
                status = #{status}
            </if>
            <if test="status == null">
                status IN (1, 2)
            </if>
            <if test="zone != null and zone != ''">
                AND location = #{zone}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectById" resultMap="LandResultMap">
        SELECT id, name, location, area, price, description, images, status, create_time, update_time
        FROM land
        WHERE id = #{id}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM land
        <where>
            <if test="status != null">
                status = #{status}
            </if>
            <if test="status == null">
                status IN (1, 2)
            </if>
            <if test="zone != null and zone != ''">
                AND location = #{zone}
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO land (name, location, area, price, description, images, status)
        VALUES (#{name}, #{location}, #{area}, #{price}, #{description}, #{images}, #{status})
    </insert>

    <update id="update">
        UPDATE land
        SET name = #{name},
            location = #{location},
            area = #{area},
            price = #{price},
            description = #{description},
            images = #{images},
            status = #{status}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE land SET status = #{status} WHERE id = #{id}
    </update>
</mapper>
