<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wisdom.farm.mapper.UserMapper">
    <resultMap id="UserResultMap" type="com.wisdom.farm.entity.User">
        <id property="id" column="id"/>
        <result property="openid" column="openid"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByOpenid" resultMap="UserResultMap">
        SELECT id, openid, nickname, avatar, phone, status, create_time, update_time
        FROM `user`
        WHERE openid = #{openid}
    </select>

    <select id="selectById" resultMap="UserResultMap">
        SELECT id, openid, nickname, avatar, phone, status, create_time, update_time
        FROM `user`
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (openid, nickname, avatar, phone, status)
        VALUES (#{openid}, #{nickname}, #{avatar}, #{phone}, #{status})
    </insert>

    <update id="updateProfile">
        UPDATE `user`
        SET nickname = #{nickname}, avatar = #{avatar}
        WHERE id = #{id}
    </update>

    <update id="updatePhone">
        UPDATE `user`
        SET phone = #{phone}
        WHERE id = #{id}
    </update>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM `user`
    </select>
</mapper>
