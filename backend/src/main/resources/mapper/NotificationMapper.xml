<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wisdom.farm.mapper.NotificationMapper">
    <resultMap id="NotificationResultMap" type="com.wisdom.farm.entity.Notification">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="read" column="is_read"/>
        <result property="extra" column="extra"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (user_id, title, content, type, is_read, extra)
        VALUES (#{userId}, #{title}, #{content}, #{type}, #{read}, #{extra})
    </insert>

    <select id="selectList" resultMap="NotificationResultMap">
        SELECT id, user_id, title, content, type, is_read, extra, create_time
        FROM notification
        <where>
            user_id = #{userId}
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM notification
        <where>
            user_id = #{userId}
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
    </select>

    <select id="unreadCount" resultType="long">
        SELECT COUNT(*) FROM notification WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markRead">
        UPDATE notification SET is_read = 1 WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="markAllRead">
        UPDATE notification SET is_read = 1 WHERE user_id = #{userId}
    </update>
</mapper>
