server:
  port: 8080

spring:
  application:
    name: wisdom-farm
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fanclub?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 1357966m

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.wisdom.farm.entity

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
