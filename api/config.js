// [BACKEND_PENDING] Replace with the real backend base URL, e.g. 'https://api.example.com'.
// When BASE_URL starts with TODO_, the request layer automatically uses mock data.
const BASE_URL = 'TODO_REPLACE_WITH_REAL_BACKEND_URL';

// Set to true to keep mock mode on after BASE_URL is filled.
const FORCE_MOCK = false;

module.exports = { BASE_URL, FORCE_MOCK };
