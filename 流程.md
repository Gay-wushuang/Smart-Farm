| 层         | 推荐                |
| :--------- | :------------------ |
| 后端框架   | Spring Boot 3.x     |
| 数据库     | MySQL (Redis先不加) |
| ORM        | MyBatis             |
| 接口规范   | Swagger             |
| 小程序框架 | 原生                |
| 代码管理   | Git（Gitee）        |

# 后端先出接口文档(Apifox)   最多一天,写完

```
git分支策略
main          ← 线上稳定版本
  └── dev     ← 开发主分支
       ├── feat/user-login     ← 功能分支
       ├── feat/order-list
       └── fix/bug-xxx         ← 修复分支

```

# 禁止直接推 dev 和 main

```
项目结构约定

backend/              ← 后端项目
  ├── src/main/java/com/xxx/
  │   ├── controller/   ← 接口层
  │   ├── service/      ← 业务层
  │   ├── mapper/       ← 数据层
  │   ├── entity/       ← 实体类
  │   └── config/       ← 配置类
  └── src/main/resources/
        ├── application.yml
        └── mapper/      ← MyBatis XML

miniapp/              ← 小程序项目
  ├── pages/           ← 页面
  ├── components/      ← 组件
  ├── api/             ← 接口请求封装
  ├── utils/           ← 工具函数
  └── app.json         ← 全局配置

```

# 写完一个模块立刻测试,推git,有问题及时交流,不要甩锅